{
    "name": "mern",
    "version": "1.0.0",
    "main": "server.js",
    "license": "MIT",
    "scripts": {
        "start": "cd client && yarn && yarn run build && cd .. && node server",
        "dev": "node server"
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "dotenv": "^10.0.0",
        "express": "^4.17.1",
        "mongoose": "^5.13.2"
    }
}
